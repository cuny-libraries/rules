rule "copy course identifier to local field 15"
##Copies identifier (not starting with "http" or "oai") metadata from Dublin Core to local field 15
when
      true
then
      copy "//*[local-name()='identifier'][not(starts-with(., 'http')) and not(starts-with(., 'oai'))]/." to "discovery"."local15"
end
